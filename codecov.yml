flag_management:
  default_rules:
    carryforward: false
  individual_flags:
    - name: "ats-test"
      carryforward: true
      carryforward_mode: "labels"

coverage:
  status:
    project: true
    patch: true

comment:
  require_changes: "coverage_drop or uncovered_patch"
